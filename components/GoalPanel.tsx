// FORE — components/GoalPanel.tsx
// Owner: TASK-006 (Drashti). Goal input form (target amount + date) + pace verdict display.
// Pace calc: goal.target_amount / days-remaining vs burn_rate.daily_avg — simple arithmetic only.
// Edge case: target_date in the past -> validation message, not a nonsensical negative.

"use client";

export default function GoalPanel() {
  // TODO(TASK-006): form for target_amount + target_date, compute on_pace + pace_gap_days,
  // write back into financial_context.goal.
  return <div>AHEAD / Goal — TASK-006 not yet implemented</div>;
}
