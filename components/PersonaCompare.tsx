"use client";

/**
 * Persona switching removed — archetypes are assigned from spending patterns
 * (RupeeIQ model). Kept as a no-op stub so old imports don't break.
 */
export default function PersonaCompare() {
  return null;
}
